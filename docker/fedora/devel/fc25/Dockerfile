from mccaskey/base-fc25-gcc
run git clone https://github.com/ORNL-QCI/xacc \
    && source /etc/bashrc && module load mpi/mpich-x86_64 && cd /projects/xacc && mkdir build \
    && cd build && cmake .. && make -j2 install
